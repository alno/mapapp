- if @query
  Search results for #{@query}

  %ul
    - @buildings.each_with_distance do |obj, distance|
      - centroid = obj.latlng_centroid

      %li.result{ 'data-lat' => centroid.y, 'data-lng' => centroid.x, 'data-zoom' => 14 }
        %a.name{ :href => '#' }= obj.name

        - if obj.respond_to?(:address) && obj.address
          %span.address= obj.address

        %span.distance= distance

- else
  No query - no results
