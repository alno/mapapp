!!!5
%html
  = render 'layouts/header'

  %body
    = render 'layouts/navbar'

    #content.map{ :class => params[:q].blank? ? "" : "with-sidebar" }
      = yield

    #sidebar{ :style => params[:q].blank? ? "left: -400px": "" }
      = render_cell :search, :results, params
