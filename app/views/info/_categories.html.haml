%ul.categories
  - model.categories.each do |cat|
    %li{ :style => "background: url(/images/icons-classic/#{cat.icon}.png) no-repeat left center" }
      %a{ :href => "#categories=#{cat.id}" }= cat.name
.clear-left
